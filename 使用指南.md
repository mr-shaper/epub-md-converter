# epub2MD 使用指南

## 📖 项目简介

epub2MD 是一个强大的 EPUB 电子书转换工具，可以将 EPUB 格式的电子书转换为 Markdown 格式，方便在 Obsidian 等 Markdown 编辑器中阅读和编辑。

**部署位置**: `/Users/mrshaper/Library/CloudStorage/OneDrive-SharedLibraries-onedrive/文档/Obsidian Vault/AI 应用/epub2MD`

## ✨ 主要功能

- **EPUB 转 Markdown**: 将 EPUB 文件转换为带序号的 Markdown 文件
- **自动校正**: 可选择自动处理中英文之间的空格和标点符号
- **章节合并**: 可选择将所有章节合并为单个 Markdown 文件，支持链接跳转
- **图片处理**:
  - 保留原始在线图片链接
  - 下载并本地化在线图片（将远程图片保存到本地）
- **信息查看**: 轻松查看 EPUB 的基本信息、结构和章节
- **提取功能**: 无需转换，直接提取 EPUB 文件中的有用内容

## 🚀 快速开始

### 全局安装（推荐用于命令行使用）

```bash
npm install epub2md -g
```

### 基本使用

```bash
# 显示帮助信息
epub2md -h

# 直接转换为 Markdown 格式
epub2md /path/to/your/book.epub

# 转换并合并为单个文件
epub2md /path/to/your/book.epub --merge

# 转换并自动校正中英文格式
epub2md /path/to/your/book.epub -M

# 下载并本地化在线图片
epub2md /path/to/your/book.epub --localize

# 转换并合并，同时本地化图片
epub2md /path/to/your/book.epub --merge --localize
```

### 批量转换

```bash
# 使用通配符转换多个文件
epub2md "books/*.epub"

# 转换特定模式的文件
epub2md "books/fiction-*.epub"

# 批量转换并为每个文件生成合并文件
epub2md "books/*.epub" --merge
```

### 信息查看

```bash
# 查看基本信息
epub2md -S /path/to/book.epub

# 查看结构信息
epub2md -s /path/to/book.epub

# 查看详细信息
epub2md -u /path/to/book.epub

# 提取 EPUB 内容
epub2md -i /path/to/book.epub
```

## 💡 使用建议

1. **对于中文书籍**: 建议使用 `-M` 参数进行自动格式校正
   ```bash
   epub2md book.epub -M --merge
   ```

2. **在 Obsidian 中使用**: 建议使用 `--merge` 参数创建单个文件，便于管理
   ```bash
   epub2md book.epub --merge=书名.md
   ```

3. **处理远程图片**: 如果转换时出现图片警告，使用 `--localize` 下载图片
   ```bash
   epub2md book.epub --localize --merge
   ```

## 📁 输出结构

- 默认情况下，转换后的文件会保存在与原 EPUB 文件同名的文件夹中
- 使用 `--merge` 参数时，会生成单个合并的 Markdown 文件
- 图片默认保存在 `images` 子文件夹中

## 🔧 开发使用

如果需要在代码中使用：

```javascript
import { parseEpub } from 'epub2md'

const epubObj = await parseEpub('/path/to/file.epub')
console.log('epub content:', epubObj)
```

## 📝 常见问题

**Q: 转换后的图片无法显示？**
A: 使用 `--localize` 参数下载远程图片到本地

**Q: 如何转换多个 EPUB 文件？**
A: 使用通配符模式，例如：`epub2md "books/*.epub"`

**Q: 如何在 Obsidian 中更好地组织转换后的文件？**
A: 建议使用 `--merge` 参数将每本书转换为单个 Markdown 文件，便于管理和搜索

## 📚 更多信息

- GitHub: https://github.com/uxiew/epub2MD
- 问题反馈: https://github.com/uxiew/epub2MD/issues

---

**部署日期**: 2025-12-11  
**版本**: 1.5.0
